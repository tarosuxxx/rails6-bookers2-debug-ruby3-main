// app/views/favorites/create.js.erb

// IDを指定して、現在のボタン要素を、新しくレンダリングしたテンプレートの内容で置き換える
$('#favorite_btn_<%= @book.id %>').html("<%= j(render('books/favorite_btn', book: @book)) %>");

// j() はエスケープ処理を行うヘルパーメソッドです