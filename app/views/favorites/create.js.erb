// app/views/likes/create.js.erb

// id="like-button-ポストID" の要素全体を、新しくレンダリングしたテンプレートで置き換える
$('#like-button-<%= @post.id %>').html("<%= j(render('likes/like_button', post: @post)) %>");

// j() は JavaScript エスケープを行うヘルパーメソッドです